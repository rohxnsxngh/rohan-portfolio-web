<!-- src/components/MarkdownContent.astro -->
<div class="markdown-content">
  <slot />
</div>

<style is:global>
  /* Base content container */
  .markdown-content {
    @apply max-w-4xl mx-auto;
    color: rgba(255, 255, 255, 0.85);
    line-height: 1.75;
  }

  /* Headers with tech aesthetic */
  .markdown-content h1 {
    @apply font-mono font-bold mt-12 mb-6 leading-tight;
    font-size: clamp(1.875rem, 4vw, 2.5rem);
    color: rgba(255, 255, 255, 0.95);
    letter-spacing: -0.02em;
    border-bottom: 2px solid rgba(255, 255, 255, 0.1);
    padding-bottom: 0.75rem;
  }

  .markdown-content h2 {
    @apply font-mono font-bold mt-10 mb-5 leading-tight;
    font-size: clamp(1.5rem, 3.5vw, 2rem);
    color: rgba(255, 255, 255, 0.95);
    letter-spacing: -0.01em;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    padding-bottom: 0.5rem;
  }

  .markdown-content h3 {
    @apply font-mono font-semibold mt-8 mb-4;
    font-size: clamp(1.25rem, 3vw, 1.5rem);
    color: rgba(255, 255, 255, 0.9);
  }

  .markdown-content h4 {
    @apply font-mono font-semibold mt-6 mb-3;
    font-size: clamp(1.125rem, 2.5vw, 1.25rem);
    color: rgba(255, 255, 255, 0.85);
  }

  .markdown-content h5 {
    @apply font-mono font-semibold mt-5 mb-2 text-lg;
    color: rgba(255, 255, 255, 0.8);
  }

  .markdown-content h6 {
    @apply font-mono font-semibold mt-4 mb-2 text-base uppercase tracking-wider;
    color: rgba(255, 255, 255, 0.7);
  }

  /* Paragraphs */
  .markdown-content p {
    @apply my-5;
    color: rgba(255, 255, 255, 0.75);
    line-height: 1.8;
    font-size: clamp(0.9375rem, 2vw, 1.0625rem);
  }

  .markdown-content p strong {
    @apply font-semibold;
    color: rgba(255, 255, 255, 0.9);
  }

  .markdown-content p em {
    @apply italic;
    color: rgba(255, 255, 255, 0.8);
  }

  /* Links */
  .markdown-content a {
    @apply font-medium underline underline-offset-2 transition-all duration-200;
    color: rgba(100, 200, 255, 0.8);
    text-decoration-color: rgba(100, 200, 255, 0.4);
  }

  .markdown-content a:hover {
    color: rgba(100, 200, 255, 1);
    text-decoration-color: rgba(100, 200, 255, 0.8);
  }

  /* Lists */
  .markdown-content ul {
    @apply my-6 ml-6 space-y-2;
    list-style-type: none;
  }

  .markdown-content ul > li {
    @apply relative pl-6;
    color: rgba(255, 255, 255, 0.75);
  }

  .markdown-content ul > li::before {
    content: "▹";
    @apply absolute left-0 font-mono;
    color: rgba(100, 200, 255, 0.6);
  }

  .markdown-content ol {
    @apply my-6 ml-6 space-y-2;
    list-style-type: decimal;
    list-style-position: outside;
  }

  .markdown-content ol > li {
    @apply pl-2;
    color: rgba(255, 255, 255, 0.75);
  }

  .markdown-content ol > li::marker {
    @apply font-mono font-semibold;
    color: rgba(100, 200, 255, 0.6);
  }

  .markdown-content li > ul,
  .markdown-content li > ol {
    @apply my-3 ml-6;
  }

  /* Blockquotes */
  .markdown-content blockquote {
    @apply my-6 pl-6 py-3 border-l-4 rounded-r italic relative;
    border-color: rgba(100, 200, 255, 0.4);
    background: rgba(100, 200, 255, 0.05);
  }

  .markdown-content blockquote p {
    @apply my-2;
    color: rgba(255, 255, 255, 0.8);
  }

  /* Code */
  .markdown-content code:not([class*="language-"]) {
    @apply px-2 py-0.5 rounded font-mono;
    font-size: 0.9em;
    background: rgba(255, 255, 255, 0.08);
    color: rgba(150, 255, 150, 0.9);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .markdown-content pre {
    @apply my-6 p-5 rounded-lg overflow-x-auto;
    background: rgba(0, 0, 0, 0.3);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .markdown-content pre code {
    @apply bg-transparent p-0 border-0 font-mono;
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.85);
    line-height: 1.7;
  }

  /* Horizontal Rule */
  .markdown-content hr {
    @apply my-12 border-t;
    border-color: rgba(255, 255, 255, 0.1);
  }

  /* Tables */
  .markdown-content table {
    @apply w-full my-8 border-collapse;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 0.5rem;
    overflow: hidden;
  }

  .markdown-content thead {
    background: rgba(255, 255, 255, 0.05);
  }

  .markdown-content th {
    @apply py-3 px-4 text-left font-mono font-semibold border-b-2;
    color: rgba(255, 255, 255, 0.9);
    border-color: rgba(255, 255, 255, 0.2);
    font-size: 0.875rem;
  }

  .markdown-content td {
    @apply py-3 px-4 border-b;
    color: rgba(255, 255, 255, 0.75);
    border-color: rgba(255, 255, 255, 0.08);
  }

  .markdown-content tr:hover {
    background: rgba(255, 255, 255, 0.03);
    transition: background 0.2s ease;
  }

  /* Images */
  .markdown-content img {
    @apply my-8 rounded-lg max-w-full mx-auto;
    border: 1px solid rgba(255, 255, 255, 0.15);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
  }

  .markdown-content figure {
    @apply my-8;
  }

  .markdown-content figcaption {
    @apply text-center text-sm font-mono mt-3 italic;
    color: rgba(255, 255, 255, 0.5);
  }

  /* Mobile responsive */
  @media (max-width: 640px) {
    .markdown-content {
      font-size: 0.9375rem;
    }

    .markdown-content h1 {
      @apply mt-8 mb-4;
      font-size: clamp(1.5rem, 6vw, 2rem);
    }

    .markdown-content h2 {
      @apply mt-7 mb-3;
      font-size: clamp(1.25rem, 5vw, 1.75rem);
    }

    .markdown-content h3 {
      @apply mt-6 mb-3;
      font-size: clamp(1.125rem, 4.5vw, 1.5rem);
    }

    .markdown-content pre {
      @apply p-3 my-4;
      font-size: 0.8125rem;
    }

    .markdown-content ul,
    .markdown-content ol {
      @apply ml-4;
    }

    .markdown-content table {
      @apply block overflow-x-auto;
      font-size: 0.875rem;
    }

    .markdown-content th,
    .markdown-content td {
      @apply py-2 px-3;
    }

    .markdown-content img {
      @apply my-6;
    }
  }

  /* Tablet adjustments */
  @media (min-width: 641px) and (max-width: 1024px) {
    .markdown-content {
      font-size: 1rem;
    }

    .markdown-content pre {
      font-size: 0.875rem;
    }
  }
</style>
